import { NotificationType } from './notification-type';
export declare class DiflexmoNotification {
    type: NotificationType;
    headerText: string;
    bodyText: string;
    sticky: boolean;
    constructor(type: NotificationType, headerText: string, bodyText?: string, sticky?: boolean);
}
