import { ElementRef, QueryList } from '@angular/core';
import { SelectItem } from '../modules/input-dropdown/models/select-item';
export declare class ItemsCompactHelper {
    static getItemsUnion(itemsUnionName?: string): SelectItem;
    static compactItems(itemsToCompact: any[], containerWidth: number, itemsGap: number, itemsElementRef: QueryList<ElementRef>, itemsUnionName?: string): {
        items: any[];
        itemsUnionIndex: number;
        containerWidth: number;
    };
    private static getItemsCalculations;
}
